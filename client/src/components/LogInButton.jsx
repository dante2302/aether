import { useState } from "react"

const LogInButton = ({isLogged,setLogged,toggleModal}) => {
  return(
    <>
      <button onClick={toggleModal}>a</button>
    </>
  )
}

export default LogInButton
